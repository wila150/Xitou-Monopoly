// 對應架構文件「九、時間控管（10:30～12:30）」
module.exports = {
  // 不會在 12:30 自動停止受理關卡進度：隊伍可能逾時才完成，逾時仍要能繼續闖關與辦理終點確認（成績標註「逾時」）。
  // 小編只有手動輸入「遊戲結束」才會停止受理新進度。
  // 12:15 系統對所有「闖關中」隊伍發送提醒通知
  reminderHour: 12,
  reminderMinute: 15,
  // 「準時／逾時」改成看該組自己出發後經過多久，不是比對固定時刻：
  // 每組從「出發」算起，2 小時內辦理終點確認都算準時，超過算逾時
  // （呼應 10:30～12:30 這個活動時間窗口的長度，但不會因為某組出發得比較晚而不公平地被判逾時）
  maxDurationMinutes: 120,
  totalCheckpoints: 12,
};
